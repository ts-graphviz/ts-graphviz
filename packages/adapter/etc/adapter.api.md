## API Report File for "@ts-graphviz/adapter"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

/// <reference types="node" />

import type { $keywords } from '@ts-graphviz/common';
import type { $keywordsValidation } from '@ts-graphviz/common';
import type { EdgeAttributesObject } from '@ts-graphviz/common';
import type { GraphAttributesObject } from '@ts-graphviz/common';
import type { NodeAttributesObject } from '@ts-graphviz/common';
import type { SubgraphAttributesObject } from '@ts-graphviz/common';

// @public
export type Format = Format.values;

// @public (undocumented)
export namespace Format {
    // (undocumented)
    export interface $exclude extends $keywordsValidation {
    }
    export interface $values extends $keywords<'png' | 'svg' | 'json' | 'jpg' | 'pdf' | 'xdot' | 'dot' | 'plain' | 'dot_json'> {
    }
    // (undocumented)
    export type values = Exclude<keyof $values, keyof $exclude | symbol | number>;
}

// @public
export type Layout = Layout.values;

// @public (undocumented)
export namespace Layout {
    // (undocumented)
    export interface $exclude extends $keywordsValidation {
    }
    // (undocumented)
    export interface $values extends $keywords<'dot' | 'neato' | 'fdp' | 'sfdp' | 'circo' | 'twopi' | 'nop' | 'nop2' | 'osage' | 'patchwork'> {
    }
    // (undocumented)
    export type values = Exclude<keyof $values, keyof $exclude | symbol | number>;
}

// Warning: (ae-forgotten-export) The symbol "CommonOptions" needs to be exported by the entry point node.d.ts
// Warning: (ae-forgotten-export) The symbol "NeatoOptions" needs to be exported by the entry point node.d.ts
// Warning: (ae-forgotten-export) The symbol "FdpOptions" needs to be exported by the entry point node.d.ts
// Warning: (ae-forgotten-export) The symbol "OtherOptions" needs to be exported by the entry point node.d.ts
//
// @beta
export type Options<T extends Layout = Layout> = CommonOptions & (T extends 'neato' ? NeatoOptions : T extends 'fdp' ? FdpOptions : OtherOptions);

// Warning: (ae-incompatible-release-tags) The symbol "toFile" is marked as @public, but its signature references "Options" which is marked as @beta
//
// @public
export function toFile<T extends Layout>(dot: string, path: string, options?: Options<T>): Promise<void>;

// @beta
export function toStream<T extends Layout>(dot: string, options?: Options<T>): Promise<NodeJS.ReadableStream>;

// (No @packageDocumentation comment for this package)

```
