{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "clean": {
      "dependsOn": [],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "rollup.config.ts", "tsconfig.json"],
      "outputs": ["lib/**"]
    },
    "@ts-graphviz/parser#build": {
      "dependsOn": ["^build"],
      "inputs": ["peggy/**", "scripts/**", "src/**", "package.json", "rollup.config.ts", "tsconfig.json"],
      "outputs": ["src/parse.js", "lib"]
    },
    "test": {
      "outputs": []
    }
  },
  "globalDependencies": ["tsconfig.json"]
}
