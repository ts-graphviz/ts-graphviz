{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["package.json", "pnpm-lock.yaml", "tsconfig.json"],
  "pipeline": {
    "codegen": {},
    "tsgraphviz#codegen": {
      "dependsOn": ["^codegen"],
      "inputs": [
        "package.json",
        "src/ast/dot-shim/parser/dot.peggy",
        "src/ast/dot-shim/parser/peggy.options.json"
      ],
      "outputs": ["src/ast/dot-shim/parser/_parse.ts"]
    },
    "//#test:unit": {
      "dependsOn": ["ts-graphviz#codegen"],
      "inputs": ["packages", "test", "vitest.config.ts"],
      "outputs": ["coverage/**"]
    },
    "test:unit": {
      "dependsOn": ["^test:unit"]
    },
    "build": {
      "dependsOn": ["codegen"],
      "inputs": [
        "src/**",
        "tsconfig.json",
        "vite.config.ts",
        "!**/*.test.ts",
        "!**/*.spec.ts"
      ],
      "outputs": ["lib/**"]
    },
    "test:integration:build": {
      "dependsOn": ["build"],
      "inputs": ["**/*.js", "**/*/ts"],
      "outputs": ["dist/**"]
    },
    "test:integration": {
      "inputs": ["**/*.js", "**/*/ts"],
      "dependsOn": ["test:integration:build"]
    }
  }
}
